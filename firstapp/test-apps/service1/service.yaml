apiVersion: v1
kind: Service
metadata:
  name: service1-service
  namespace: argocd
  annotations:
    service.beta.kubernetes.io/gcp-load-balancer-internal: 'true'
spec:
  ports:
  - name: TCP
    port: 80
    targetPort: 8080
  type: NodePort	  
